services:
  app:
    build: ./docker
    container_name: Aurora
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/html
      - ./docker/php.ini:/usr/local/etc/php/php.ini:ro
    restart: unless-stopped